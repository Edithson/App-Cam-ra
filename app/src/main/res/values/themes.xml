<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AppCamera1" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- <item name="android:buttonStyle">@style/Theme.AppCamera1</item> -->
    </style>

    <style name="Theme.AppCamera1" parent="Base.Theme.AppCamera1">

    </style>
    <style name="MyTheme" parent="Base.Theme.AppCamera1">
        <item name="android:textColor">@color/white</item>
        <item name="android:backgroundTint">@color/black</item>
        <item name="android:layout_marginLeft">18dp</item>
        <item name="android:textSize">16dp</item>
        <item name="android:padding">0dp</item>
    </style>

    <style name="MyTheme2" parent="Base.Theme.AppCamera1">
        <item name="android:textColor">@color/white</item>
        <item name="android:backgroundTint">@color/ColoerBtn</item>
        <item name="android:layout_marginLeft">30dp</item>
        <item name="android:textSize">20dp</item>
    </style>
</resources>